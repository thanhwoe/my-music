export interface ISong {
    artist: string
    filePath: string
    id: string
    provider: string
    source: string
    thumbnail: string
    title: string
    user: {
        username: string
    }
}